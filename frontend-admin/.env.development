# 应用端口
VITE_APP_PORT=3000
# Axios baseURL（空 = 使用相对路径，由 Vite 代理转发 /api）
VITE_APP_BASE_API=

# 智考云后端接口地址
VITE_APP_API_URL=http://localhost:8080

# 启用 Mock 服务(true:开启 false:关闭)
VITE_MOCK_DEV_SERVER=false
